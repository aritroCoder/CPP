#include <bits/stdc++.h>

#define fastio                        \
    ios_base::sync_with_stdio(false); \
    cin.tie(NULL)

#define endl "\n"

using namespace std;

int main()
{
    fastio;
    int M,N;
    cin >> M >> N;
    pair<int,int> p[M]; //key and door-lock
    int locks[N];
    for (int i = 0; i < M; i++)
    {
        cin >> p[i].first() >> p[i].second() ;
    }
    for (int i = 0; i < N; i++)
    {
        
    }
    
    
    return 0;
}