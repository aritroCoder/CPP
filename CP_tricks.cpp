#include<bits/stdc++.h>
using namespace std;

int main(){
    // -10^9 < int < 10^9;
    // -10^12 < long int < 10^12;
    // -10^18 < long long int < 10^18;
    // double and float have precision errors so dont use them if not reqd in CP 
    for (  ;   ; )
    {
        /* code */
    }
    //use cpst for getting starting template
    //use cin.ignore() to flush buffers. Use it before using getline()
    //use str.push_back(char) to push a char after a string
    // str.erase(str.begin() + 4); where 4 is pos to erase (erases one char)
    //sort the array in ascending order
    // sort(a, a+n); n is the length upto which it is sorted
    //you can now create dynamic arrays in c++ using cin>>n; int a[n]; 
    //fill(arr, arr+4, 0); to initialise global array as zero, where 4 is stricly the SIZE of the array
}