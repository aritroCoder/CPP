#include <bits/stdc++.h>

#define fastio                        \
    ios_base::sync_with_stdio(false); \
    cin.tie(NULL)

#define endl "\n"

using namespace std;

int main()
{
    fastio;
    int t;
    cin >> t;
    
    while(t--)
    {
       int N, A;
       cin >> N >> A;
       int C[N], D[N];
       for (int i = 0; i < N; i++)
       {
           cin >> c[i] >> d[i];
       }
       
       
    }
    return 0;
}